#include <MsTimer2.h>

long previousTime; 
int x;

void setup() 
{ 
  previousTime = millis();
  Serial.begin(9600);
}

void loop() 
{ 
 if((millis() - previousTime) > 1000) 
 {  // if 5 mins have passed since started counting 
   x++;
   Serial.print(x);
   previousTime = millis();
 }
}
